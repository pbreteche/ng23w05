import { PeriodDirective } from './period.directive';

describe('PeriodDirective', () => {
  it('should create an instance', () => {
    const directive = new PeriodDirective();
    expect(directive).toBeTruthy();
  });
});
